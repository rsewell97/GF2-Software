devices {
A is a XOR;
B is a NAND;
S1, S2 are SWITCH;
(S1 starts high;)
(CLK1 has cycle 5;)
}
connections {
   device A {
       S1 to A.1;
       B to A.2;
   }
   device B {
       A to B.1;
       S2 to B.2;
   }
}
monitor {
   A, B;
}
